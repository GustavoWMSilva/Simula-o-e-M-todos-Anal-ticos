# T1 - Simulador de filas

## Setup

Para uso do leitor de arquivos .yml, utilize o comando:

` $ pip install pyyaml`

## Run 

Para executar a aplicação, utilize o comando:

`$ python3 main.py <nome-arquivo>.yml`